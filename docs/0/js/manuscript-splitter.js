(function(){
class Manuscript {// 原稿（読込、解析、妥当性確認、デフォルトフォーマット出力）
    static load(...args) {
        // 文字列（平文の原稿）
        // Base64URL（平文の原稿をCompression APIで圧縮したバイナリをBase64URL化したテキスト）
    }
    constructor(...args) {
        this._str = null; // 原稿（文字列データ）
    }
}
})();
